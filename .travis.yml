language: rust
script:
  - cargo build --verbose
  - cargo build --verbose --features "resistance derp democracy"
  - cargo test --verbose
  - cargo test --verbose --features "resistance derp democracy"
notifications:
  irc:
    channels:
      - "irc.fyrechat.net#vana-commits"
    template:
      - "%{repository_slug}/%{branch} (%{commit} - %{author}): %{message}"
    skip_join: true
